<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/admity_bacground_img"
    tools:context=".views.EmergencyNoticeActivity">
    <include layout="@layout/layout_toolbar"
        android:id="@+id/emergency_notice_layout_toolbaar"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/emergency_notice_recyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/emergency_notice_layout_toolbaar"/>

</RelativeLayout>