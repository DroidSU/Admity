<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/admity_bacground_img"
    tools:context=".views.MyWarningActivity">
    <include layout="@layout/layout_toolbar"
        android:id="@+id/my_warning_layout_toolbar"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/my_warning_recyclerView"
        android:layout_below="@id/my_warning_layout_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</RelativeLayout>